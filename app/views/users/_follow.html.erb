<%= form_with(
     model: get_current_user_or_nil.active_relationships.build,
     local: false
) do |f| %>
  <div>
    <%= hidden_field_tag(:followed_id, @user.id) %>
    <%= f.submit("Follow", { class: "btn btn-primary" }) %>
  </div>
<% end %>